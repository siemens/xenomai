OPTDIRS =

if CONFIG_XENO_MVM_SKIN_PSOS
OPTDIRS += psos+
endif

if CONFIG_XENO_MVM_SKIN_VXWORKS
OPTDIRS += vxworks
endif

if CONFIG_XENO_MVM_SKIN_VRTX
OPTDIRS += vrtx
endif

if CONFIG_XENO_MVM_SKIN_UITRON
OPTDIRS += uitron
endif

if CONFIG_XENO_MVM_SKIN_POSIX
OPTDIRS += posix
endif

if CONFIG_XENO_MVM_SKIN_NATIVE
OPTDIRS += native
endif

if CONFIG_XENO_MVM_SKIN_RTAI
OPTDIRS += rtai
endif

SUBDIRS = $(OPTDIRS)
