OPTDIRS =

if CONFIG_XENO_OPT_PERVASIVE
if CONFIG_XENO_SKIN_POSIX
OPTDIRS += cruncher
endif
if CONFIG_XENO_SKIN_NATIVE
OPTDIRS += latency switch
endif
endif

if CONFIG_XENO_SKIN_NATIVE
OPTDIRS += klatency
endif

SUBDIRS = $(OPTDIRS)
